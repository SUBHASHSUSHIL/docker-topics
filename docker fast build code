This two line of code in fast push project in docker hub

docker build -t <Project name> .

docker buildx build --platform linux/amd64,linux/arm64 --tag subhashsushil32189/<Project name>:1.0 --push .
